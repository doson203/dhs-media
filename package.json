{
  "name": "dhs-media-sales-web",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page and admin CMS for selling desktop apps.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node build-static.js",
    "check": "node --check server.js && node --check build-static.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.0.2"
  }
}
